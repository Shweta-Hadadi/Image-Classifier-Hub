# 🖼️ Image Classifier Hub

A deep learning–based web app that classifies uploaded images using **MobileNetV2** pre-trained on ImageNet.

## 🚀 Project Overview
This project allows users to upload an image and view the top 5 predicted categories along with confidence levels using a pre-trained CNN model.

## 🧰 Tech Stack
- Python 3.10+
- TensorFlow / Keras
- NumPy
- Matplotlib
- Google Colab (for running)
- (Optional) FastAPI / Flask (for deployment)

## ⚙️ Setup Instructions
1. Clone this repository:
   ```bash
   git clone https://github.com/<your-username>/Image-Classifier-Hub.git
   cd Image-Classifier-Hub
